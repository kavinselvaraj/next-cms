node_modules
dist
*.tsbuildinfo

# Runtime state — regenerated by the tool, not authored by hand.
# Commit data/mapping.json and data/asset-mapping.json deliberately once you
# have a real run's output worth versioning (see README, "POC mapping
# storage") — they are NOT ignored here on purpose, only the lockfile is.
data/*.lock
snapshots/
reports/
.cache/

.env
.env.local
